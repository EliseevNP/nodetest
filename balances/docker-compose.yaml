version: '3'
services:
  balances_backend:
    build: .
    ports:
      - "3001:3001"
    environment:
      MONGO_HOST: 'localhost'
      RABBIT_HOST: 'localhost'
      RABBIT_PORT: 10000
    depends_on:
      - mongo
    network_mode: "host"
  mongo:
    image: "mongo:4-bionic"